@using ApplicationCore.ServiceContracts
@inject IGenreService GenreService

@foreach (var genre in await GenreService.GetAllGenres())
{
    @*@Html.ActionLink(genre.Name, "Genre", "Movies", new { genreId = genre.Id }, new { @class = "dropdown-item" })*@

    <a asp-action="genreMovies" asp-controller="Movies" class="dropdown-item" asp-route-id="@genre.Id"> @genre.Name</a>
}